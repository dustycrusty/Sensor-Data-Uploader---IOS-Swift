# Uncomment the next line to define a global platform for your project
platform :ios, '14.5'

target 'sensorUpload' do
  # Comment the next line if you don't want to use dynamic frameworks
  inhibit_all_warnings!

  use_frameworks!
  # Pods for sensorUpload
   
    pod 'Firebase/Auth'
    pod "MetaWear", :subspecs => ['UI', 'AsyncUtils', 'Mocks']
    pod 'iOSDFULibrary', :git => 'https://github.com/NordicSemiconductor/IOS-DFU-Library.git', :branch => 'develop'
    pod 'Firebase/Database'
    pod 'MBProgressHUD'
    pod 'StaticDataTableViewController'
    
end
